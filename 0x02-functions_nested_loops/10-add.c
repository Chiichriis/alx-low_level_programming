#include "main.h"
#include <stdio.h>
/**
 * add - adds two integers
 * @x: int type num
 * @y: int tupe num
 * Return: the result
 */
int add(int x, int y)
{
	int result = x + y;

	return (result);
}
