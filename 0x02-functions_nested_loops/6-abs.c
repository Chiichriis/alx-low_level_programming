#include "main.h"
/**
 * _abs - find absolute value of an integer
 * @n: int type num
 * Return: absolute value of num
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (n * -1);
	}
	else
	{
		return (n);
	}
}
